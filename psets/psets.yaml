---
# Document defines the list of psets, by name, including scaffolding and assertions. 
meta:
  updated: 2019.03.17
  location: https://raw.github.com/alpha-bytes/zs50-content/psets/list.md

psets:
  sayHello: 
    scaffold: |
      "public class ZS50_SayHello {
    
        public String sayHi(){
          // TODO initialize a string with value 'Hello, World!' 
          // write it to Apex debug logs
        }

        // implement a public method that: 
        // 1: returns a string
        // 2: takes a single String as an argument
        // 3: writes the argument to the debug logs, and returns the same

      }"

    

...